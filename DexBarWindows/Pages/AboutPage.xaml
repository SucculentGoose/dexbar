<Page x:Class="DexBarWindows.Pages.AboutPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Background="Transparent"
      FontFamily="Segoe UI">

    <Grid>
        <StackPanel VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Margin="40">

            <!-- App name -->
            <TextBlock Text="DexBar"
                       FontSize="32"
                       FontWeight="Bold"
                       Foreground="#F0F0F2"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,8" />

            <!-- Version -->
            <TextBlock x:Name="LblVersion"
                       FontSize="13"
                       Foreground="#A0A0A8"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,4" />

            <!-- Edition -->
            <TextBlock Text="Windows Edition"
                       FontSize="13"
                       Foreground="#64646C"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,32" />

            <!-- GitHub link -->
            <Button x:Name="BtnGitHub"
                    Content="View on GitHub"
                    HorizontalAlignment="Center"
                    Width="160"
                    Height="36"
                    Background="Transparent"
                    Foreground="#0A84FF"
                    FontSize="13"
                    Cursor="Hand"
                    Click="BtnGitHub_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="BtnBorder"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="#0A84FF"
                                            BorderThickness="1"
                                            CornerRadius="6">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="BtnBorder" Property="Background" Value="#0A84FF1A" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>

        </StackPanel>
    </Grid>

</Page>
